clc
clear
while(1)
clc
clear all
tcpipClient = tcpip('127.0.0.1',55002,'NetworkRole','Client');
set(tcpipClient,'Timeout',30);
fopen(tcpipClient);
x=0;
y=-1;
fwrite(tcpipClient,[num2str(x),'b',num2str(y)]);
delete(instrfindall)
end
...fclose(tcpipClient);


½
Matlab is used as client and Unity is used as server.
½

